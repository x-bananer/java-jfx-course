package demo;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
        // HelloWorldApplication.launch(HelloWorldApplication.class);

        // BorderPaneExample.launch(BorderPaneExample.class);
        // GridPaneExample.launch(GridPaneExample.class);
        // HBoxExample.launch(HBoxExample.class);

        // VBoxExample.launch(VBoxExample.class);

        // StackPaneExample.launch(StackPaneExample.class);

        // AnchorPaneExample.launch(AnchorPaneExample.class);

        // FlowAndTilePane.launch(FlowAndTilePane.class);

        // CSSExample.launch(CSSExample.class);
        CanvasExample.launch(CanvasExample.class);
    }
}